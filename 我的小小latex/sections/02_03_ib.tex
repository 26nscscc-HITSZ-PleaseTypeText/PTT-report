\subsection{指令缓冲（IB）模块设计}

指令缓冲用于实现前后端解耦，使前端取指与后端执行互不阻塞。
IB 深度为 16，每拍最多推入 4 条指令、弹出 2 条指令供译码使用。
每项包含：指令有效位、PC、指令码、分支预测信息以及取指阶段附着的例外信息等。

由于入队宽度可变，IB将空闲项不足 4 时视为满，反压前端取指，
而队列为空时向后端给出无效气泡，保证流水握手完整。
此外，IB 写入口与译码/重命名 ready 解耦，避免冷启动时因后端尚未就绪而损失本可同拍消费的取指带宽。
